terraform {
  backend "s3" {
    region         = "us-west-2"
    bucket         = "terraform-remote-state"
    key            = "webserver/state.tfstate"
    dynamodb_table = "terraform-state-lock"
    encrypt        = true
  }
}
